# AGENTS.md

> **这是模板。** 复制到新项目后，把 `<>` 占位符换成真实内容，
> 并删掉本行与所有 `<!-- -->` 注释。
>
> 写法说明见模板来源仓库的 `docs/agents-md-guide.md`（复制后本行可删）。

## 注意事项

1. 每次改动完成后都必须创建一个对应的 Git commit，以便后续追踪和回滚。
2. 每次改动后都必须编写和更新相关测试，并在交付前确保所有的测试和验证全部通过。

<!--
只放 2-3 条最高优先级、不可协商的约束。
放多了会稀释——当所有事都重要时，没有事重要。
-->

## 验证门禁

交付前必须跑通：

```
npm run verify
```

它等于 `<typecheck && test && e2e>`。
**不接受"单测过了就行"** —— 任何一项红都算没做完。

<!--
把 <...> 换成真实的命令组合。
如果项目还没有 e2e，就写 typecheck && test，等有了再加。
-->

## 环境前提

<!--
写 AI 猜不到的环境信息。判据：AI 能不能从代码里推断出来？能推断的就不用写。

典型内容：
- 运行时不在默认 PATH（如 Node 装在 D:\tools\node）
- shell 限制（如 PowerShell 5.1 不支持 &&、||、??、?.）
- 平台差异（如只在 Windows 打包）
- 需要特殊环境变量才能跑的命令
-->

- `<运行时路径与 PATH 设置>`
- `<shell 与平台限制>`

## 架构红线

<!--
先留空。第一条红线应该来自第一次真实的架构破坏，而不是开工前的想象。

写的时候，每条必须能被判定——grep、编译器、或测试能发现违反。
判据：AI 怎么知道自己违反了这条？答不上来就重写。

典型条目（按需取用，不要全抄）：
- 依赖方向只能向内：A / B → C → 无。C 不得依赖 <框架>，需要外部能力时通过 ports 注入。
- 新增接口必须同时改 N 处：<文件1>、<文件2>、<文件3>。
- 所有路径操作必须经过 <校验函数>。
- 数据写入必须原子（tmp + rename）；损坏数据备份改名而不是删除。
-->

（暂无。踩到第一个坑时再写第一条。）

## 经验库

[docs/lessons/](./docs/lessons/README.md) 存放踩过的坑与验证过的做法，供人和 AI 共同学习。

与本文的分工：**本文是必须遵守的规则，经验库是为什么这么定的经验。**
规则会被执行，经验会被参考。改动前先扫一眼索引，看看有没有相关的既有结论。

每条经验按「现象 / 根因 / 结论 / 反例」四节写，格式由
`tests/unit/repo/lessons.test.ts` 守卫。踩到新坑时追加一条，编号只增不改。

### 什么时候必须追加一条

满足**任意一条**就要追加，不要等别人提醒：

1. 你修掉了一个 bug，而**第一次尝试的修法是错的**（改完没通过，换了另一种才通过）。
2. 你发现某处代码/文档的写法**与直觉相反**，且不写下来下一个人还会踩。
3. 你因为**缺少某条信息**而做错了事（比如不知道某个约定、误读了某个字段）。
4. 门禁红了，而红的原因**不是本次改动引入的**（环境、依赖、平台差异）。

不满足以上任意一条时**不要写**。经验库的价值来自稀缺性，
凑数的条目会让索引变长、让真正有用的那几条被淹没。

写之前先扫一遍索引：已有条目能覆盖的，**改那一条**（在「现象」里补新证据），
不要新开一条。只有结论本身变了才新开号，并在新条目里指回旧号。

<!--
上面这段是"让 AI 真的会写"的关键。没有可对照的触发条件时，
AI 会永远认为自己没踩到坑，经验库就永远是空的。

如果项目长到需要子 Agent，可以再加一个只读的策展人，格式参考：

## 子 Agent 定义

`.claude/agents/` 下是本项目的子 Agent 定义：

| 定义 | 用途 |
| --- | --- |
| [lessons-curator.md](./.claude/agents/lessons-curator.md) | 判断本次改动是否值得沉淀成经验（只读，不落盘） |

**这些子 Agent 都没有命令执行权限。** 它们只产出文件与「给人类执行的命令清单」，
跑命令、验证绿灯、真正提交这三件事一律由人类（或主 Agent 代跑）完成。
凡是把 `Bash` 之类执行能力写进 `tools:` 的定义都会被判红。
-->
