# 经验库

> **这是模板。** 复制到 `<新项目>/docs/lessons/README.md` 后，
> 把 `<>` 占位符换成真实内容，并删掉本行与所有 `<!-- -->` 注释。

这里存放**踩过的坑**与**验证过的做法**，供人和 AI 共同学习。

与 [AGENTS.md](../../AGENTS.md) 的分工：

| | AGENTS.md | docs/lessons/ |
| --- | --- | --- |
| 内容 | 必须遵守的**规则** | 为什么这么定的**经验** |
| 形态 | 短，每次对话都注入 | 一条一文件，按需读取 |
| 变更 | 少 | 持续追加 |

规则会被执行，经验会被参考。**不要把长篇论证塞进 AGENTS.md**，那会挤占 AI 的上下文；
也不要指望经验库能约束行为，它只是参考。

## 为什么一条一文件

单文件经验库会随条目增长变成巨型文档，AI 无法一次装载，只能靠 grep 找标题。
一条一文件让 AI 能按需只读相关的那几条。

<!--
如果本项目已有巨型文档的实例，在这里点名，比抽象说理更有说服力。
-->

## 写作规范

文件名：`NNNN-kebab-case.md`，四位序号，从 `0001` 起，**只增不改号**。

每条必须包含这四节，缺一不可（由 `tests/unit/repo/lessons.test.ts` 守卫）：

```markdown
# NNNN 一句话结论

## 现象
具体发生了什么。带文件名、带报错原文、带数字。

## 根因
为什么会这样。要能解释"为什么当时没发现"。

## 结论
可判定的做法。**必须能被检查**——要么编译期能红，要么测试能红，
要么能写成一条 grep 规则。写不出可判定结论的，说明还没想清楚。

## 反例
明确不该做什么。AI 最容易犯的错不是"不知道该做什么"，
而是"不知道不该做什么"。
```

### 通用原则 + 本项目实例

每条经验都按这个结构写：**结论是通用的，现象与根因是本项目的**。
这样既能复制到其他仓库，又保留了"当初为什么这么定"的上下文。

### 修正旧条目

如果某条经验后来被证明是错的，**不要删**——改成新编号写一条修正，
并在旧条目里加一行指向它。经验库的价值在于记录"当时为什么这么想"，
删掉就丢了这个信息。

## 索引

<!--
每加一条经验，在这里登记一行。守卫会检查索引与文件一致。

注意：占位行不要写成 markdown 链接，否则守卫会把它当成真实链接去校验。
写第一条经验时，把下面这行替换成真实链接，例如：
| [0001](./0001-required-param-over-default.md) | 需要区分环境的参数设为必填 | 类型 |
-->

| 编号 | 结论 | 主题 |
| --- | --- | --- |
| 0001 | （写第一条经验时替换本行） | （主题） |

## 相关文档

<!--
复制到新项目后，这一节要么删掉，要么改成指向新项目自己的文档。
下面两条指向的是模板来源仓库，在新项目里不存在，留着会让守卫报断链。

- AGENTS.md 写作指南 —— 怎么给新项目写 AGENTS.md
- 新项目开工清单 —— 复制哪些文件、按什么顺序做
-->
